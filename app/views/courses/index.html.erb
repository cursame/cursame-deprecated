<div class="container" style="margin-top:100px;">

  <h1><%= t('.title') %></h1>

  <ul>
    <% @courses.each do |course| %>
      <li class="course">
      <%= course.name %><br/>
      <%= course.description %>
      </li>
    <% end %>
  </ul>

</div>

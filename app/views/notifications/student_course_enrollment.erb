<div class="img">
  <a href="<%= course_path(course) %>">
    <img src="<%= course.course_logo_file.xxsmall.url %>" rel="tip" title="<% course.name %>">
  </a>
</div>

<a href="<%= user_path(student) %>"><%= student.name %></a> <%= I18n.t('notifications.wants_to_participate_in_course') %>
<a href="<%= course_path(course) %>"><%= course.name %></a>.
<a href="<%= members_for_course_path(course) %>"  class="notification-link"><%= I18n.t('notifications.show_requests') %> &gt;&gt;</a>

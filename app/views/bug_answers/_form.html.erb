<%= form_for(@bug_answer) do |f| %>
  <% if @bug_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug_answer.errors.count, "error") %> prohibited this bug_answer from being saved:</h2>

      <ul>
      <% @bug_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :container %><br />
    <%= f.text_area :container %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>

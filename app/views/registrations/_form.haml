= semantic_form_for resource, :as => resource_name, :url => url do |form|
  = form.inputs :basic do
    = form.input :email
    = form.input :password
    = form.input :password_confirmation

  = form.buttons do
    = form.commit_button :button_html => {:id => :register}


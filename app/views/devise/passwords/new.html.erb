<% content_for :head do %>
  <style>
    body {
      margin-top: 80px;
    }
  </style>
<% end %>

<div class="container">
  <div class="content">
    <div class="row">
      
      <div class="span3">
        
      </div>
      
      <div class="span10" style="border:0;">
        <div class="well">
          <h2>¿Olvidaste tu contraseña?</h2>
          <%= semantic_form_for resource, :as => resource_name, :url => password_path(resource_name), :html => {:method => :post} do |form| %>
            <%= devise_error_messages! %>
            <%= form.inputs :email %>
            <%= form.buttons do %>
              <%= form.commit_button "Restablecimiento de la contraseña", :button_html => {:class => "btn", :style => "margin-left:95px;"} %>
            <% end %>
          <% end %>
          <%= render :partial => "devise/shared/links" %>
        </div>
      </div>

    </div>
  </div>
  <%= render :partial => "shared/footer" %>
</div>
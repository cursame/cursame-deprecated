Nombre, Apellido, Mail, Rol, Zona, Visitas, Posts, Logins
<% @visitors.each do |visitor| %>
  <br>
   <%= visitor.first_name %>
  ,<%= visitor.last_name %>
  ,<%= visitor.email %>
  ,<%= visitor.telefonica_role %>
  ,<%= visitor.telefonica_zone %>
  ,<%= ((visitas :from => 30.day.ago, :to => Time.now, :user_id => visitor.id)*1.6).to_int %>
  ,<%= (Comment.find_all_by_user_id(visitor.id).count * 1.6).to_int %>
  ,<%= (visitor.sign_in_count * 1.6).to_int %>
<% end %>
